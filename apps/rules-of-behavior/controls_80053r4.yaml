id: controls_80053r4
title: 800-53r4 Controls
version: 0.1

introduction:
  format: markdown
  template: |
    Related 800-53r4 Controls 

    Here are your Rule of Behavior control implementation narratives.

    {% if project.general_info.exists != "yes" %}
    You said your organization's Rules of Behavior don't exist. When you complete your Rules these narratives will update.
    {% endif %}

output:

- id: instructions
  title: instructions
  format: markdown
  display: top
  template: |

    # Related 800-53r4 Controls 

    Here are your Rule of Behavior control implementation narratives.

    {% if project.general_info.exists != "yes" %}
    You said your organization's Rules of Behavior don't exist. When you complete your Rules these narratives will update.
    {% endif %}

- id: ssp_nist80053r4_AC_2
  title: NIST 800-53r4 AC-2 Control Implementation
  format: markdown
  template: |
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    The Rules of Behavior {% if project.general_info.exists == "yes" %}describes{% else %}will describe{% endif %} users' responsibilities and expected behavior when using {{organization}} IT resources.

    The Rules of Behavior {% if project.general_info.exists == "yes" %}define{% else %}will define{% endif %} that the system administrator, based on need-to-know, assigns the proper permissions to system users.

    The Rules of Behavior {% if project.general_info.exists == "yes" %}define{% else %}will define{% endif %} that the employee’s manager approves the access rights before the initial account is created by signing off on the forms.

    All users (privileged and program account users) requesting access to the IT System {% if project.general_info.exists == "yes" %}are{% else %}will be{% endif %} required to read the IT System Rules of Behaviors, complete an account request form and sign both documents. User access is controlled by the completion and submission of IT System Rules of Behavior and New User Account Request forms by the user and management. These items {% if project.general_info.exists == "yes" %}are{% else %}will be{% endif %} completed and submitted whenever a new user requires access or an existing user requires access changes.

    {{project.general_info.obtain}}

- id: ssp_nist80053r4_AT_2
  title: NIST 800-53r4 AT-2 Control Implementation
  format: markdown
  template: >+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    The Rules of Behavior of the IT System {% if project.general_info.exists == "yes" %}requires{% else %}will require{% endif %} Department personnel and contractor employees involved with the management, operation, programming, maintenance, or use of IT System receive training in acceptable computer security practices prior to system access.

- id: ssp_nist80053r4_SA_7
  title: NIST 800-53r4 SA-7 Control Implementation
  format: markdown
  template: |+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    {{organization}} {% if project.general_info.exists == "yes" %}uses{% else %}will use{% endif %} the Rules of Behavior to document and set forth appropriate activities on an information system.

- id: ssp_nist80053r4_MP_1
  title: NIST 800-53r4 MP-1 Control Implementation
  format: markdown
  template: >+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    Media protection policy and procedures {% if project.general_info.exists == "yes" %}are{% else %}will be{% endif %} documented in System Security Plan and the IT System Rules of Behavior. These documents provide the roles and responsibilities for media protection policies and procedures as it pertains to clearing, sanitization, disposal, and storage of media.

- id: ssp_nist80053r4_PL_4
  title: NIST 800-53r4 PL-4 Control Implementation
  format: markdown
  template: >+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    All users (privileged and program account users) requesting access to the IT System {% if project.general_info.exists == "yes" %}are{% else %}will be{% endif %} required to read and sign the IT System's Rules of Behaviors. The Rules of Behavior describe the user’s responsibilities and expected behavior when using the IT System.

- id: ssp_nist80053r4_PS_6
  title: NIST 800-53r4 PS-6 Control Implementation
  format: markdown
  template: >+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    The Department {% if project.general_info.exists == "yes" %}has implemented{% else %}will implement{% endif %} a formalized process for user account administration which requires all new users requesting access to the IT System read and agree to the Rules of Behavior.

- id: ssp_nist80053r4_PS_7
  title: NIST 800-53r4 PS-7 Control Implementation
  format: markdown
  template: >+
    {% if project.general_info.exists != "yes" %}The following controls will be in place with the completion of the Rules of Behavior.{% endif %}

    All contractor support personnel are required to meet the same personnel security policy requirements as Department personnel who have privileged access to the IT System. Third party providers {% if project.general_info.exists == "yes" %}are required{% else %}will be required{% endif %} to review and sign the Rules of Behavior prior to being granted access to the system.
